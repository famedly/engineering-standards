services:
  postgres:
    image: postgres:17
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: standards
      POSTGRES_PASSWORD: standards
      POSTGRES_DB: engineering_standards
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
